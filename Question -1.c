#include <stdio.h>
	int main()
	{
	int a=8,b=10,m,n,o,p,q;
	m=a+b;
	n=a-b;
	o=a*b;
	p=a/b;
	q=a%b;
	printf("sum of two numbers is %d\n", m);
	printf("difference of two numbers is %d\n", n);
    printf("multiple of two numbers is %d\n", o);
	printf("division of two numbers is %d\n", p);
	printf("modulo of two numbers is %d\n", q);
    return 0;
}
